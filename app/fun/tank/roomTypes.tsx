export type generalInfo = {
    roomWidth: number;
    amountOfLanes: number;
}

export type tankStats = {
    width: number;
    x: number;
    y: number;
    speed: number
}

export type keysCurrentlyPressed = {
    up: boolean,
    down: boolean,
    left: boolean,
    right: boolean,
}